//var len = point_distance(x, y, target_x, target_y);
//var angle = point_direction(x, y, target_x, target_y);

//draw_sprite_ext(
//sprite_index,
//image_index,
//x, y,
//len / sprite_width,  // x-scale stretches the sprite to the line length
//1,                   // y-scale stays the same
//angle,
//image_blend,
//image_alpha
//);